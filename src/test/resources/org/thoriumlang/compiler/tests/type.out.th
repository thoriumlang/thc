type MyType {
  private a(): (D | ((C | (B & (A)?)))?);
  private b(): (A & B);
  private c(): (C & (A)?);
  private d(): (A)?;
  private e(): ((A & B))?;
  private f(): (A)?;
  private g(): (A & B & C & D);
  private h(): (A | B | C | D);
  private i(i: Integer): None;
  private j(i: (Integer)?): None;
  private k(i: (Integer)?, j: (A & (B)?)): None;
}
