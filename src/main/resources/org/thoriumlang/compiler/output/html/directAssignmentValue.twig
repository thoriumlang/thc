<div class="inline" id="{{ nodeId }}"
     data-line="{{ line }}"
     data-char="{{ char }}"
     data-kind="{{ nodeKind }}"
     data-referencedNodeId="{{ referencedNodeId }}"
>{{ identifier }} = {{ value }}</div>